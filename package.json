{
  "name": "tardis-x",
  "version": "0.1.0",
  "description": "## Deploy ``` $ polymer build $ firebase deploy ```",
  "main": "index.js",
  "scripts": {
    "build": "polymer build",
    "clean": "rm -rf bower_components && rm -rf node_modules && rm -rf build",
    "deploy": "firebase deploy",
    "deploy:functions": "firebase deploy --only functions",
    "deploy:hosting": "firebase deploy --only hosting",
    "postinstall": "bower install",
    "preinstall": "rm -rf bower_components && rm -rf node_modules",
    "start": "polymer serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Tardis-x/web.git"
  },
  "author": "Oleksandr Pidlisnyi <oleksandr.pidlisnyi@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Tardis-x/web/issues"
  },
  "private": true,
  "homepage": "https://github.com/Tardis-x/web#readme",
  "dependencies": {
    "polymer-cli": "^0.18.0-pre.12",
    "redux": "^3.6.0"
  }
}
